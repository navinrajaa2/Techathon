{
  "name": "employee-learning-platform",
  "version": "1.0.0",
  "description": "Employee Learning Recommendation & Adaptive Roadmap Platform (PathCraft AI)",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "start": "npm --prefix server start",
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev",
    "build:client": "npm --prefix client run build",
    "test:server": "node server/index.js"
  }
}
